
let $creators :=
(
    for $i in json-file("{JSONL}", 10)
    where $i.id eq "{ID}"
 return $i.creator)

let $creator :=
(for $c in $creators[]
return $c)

return [
        {
            "title": $creator,
            "link": null
        }
    ]
   