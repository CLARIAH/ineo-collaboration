(: This query check if 'url' is an object (dictionary) or a string with a 'url' property in the codemeta file.
    If there is not a url key in the codemeta (e.g. for alud) it returns the value of the codeRepository :)


for $i in json-file("{JSONL}", 10)
where $i.identifier eq "{ID}"
return
    if ($i.url instance of object and exists($i.url.url)) then $i.url.url
    else if (exists($i.url)) then $i.url
    else if (exists($i.codeRepository)) then $i.codeRepository
    else ()

